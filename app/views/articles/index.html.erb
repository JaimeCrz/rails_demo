<ul>
  <% @articles.each do |article| %>
    <li>
      <%= link_to article.title, article_path(article) %><br />
    </li>
  <% end %>
</ul>

<button type="button" onclick= "window.location.href = '' >New Article</button>